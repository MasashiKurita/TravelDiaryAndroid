<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="com.martymarron.traveldiaryandroid.CreateStoryActivity$PlaceholderFragment" >

    <EditText
        android:id="@+id/storyTitleText"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentStart="true"
        android:layout_alignParentTop="true"
        android:layout_marginStart="35dp"
        android:layout_marginTop="40dp"
        android:ems="10"
        android:hint="Title..." >

        <requestFocus />
    </EditText>

    <EditText
        android:id="@+id/storyDescriptionText"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignLeft="@+id/storyTitleText"
        android:layout_below="@+id/storyTitleText"
        android:layout_marginTop="64dp"
        android:ems="10"
        android:hint="Description..."
        android:inputType="textMultiLine" />

    <Button
        android:id="@+id/createStoryButton"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignLeft="@+id/storyDescriptionText"
        android:layout_alignRight="@+id/storyDescriptionText"
        android:layout_below="@+id/storyDescriptionText"
        android:layout_marginTop="63dp"
        android:text="Create"
        android:onClick="initStory" />

</RelativeLayout>
